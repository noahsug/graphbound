Original prompt: Create a GitHub repo for this web game project, write a detailed implementation plan in `PLAN.md`, push that plan to the remote repo, implement v0 using the game-development skill, and keep both `PLAN.md` and `PROGRESS.md` updated as work progresses. The game is a hand-drawn-feeling open-world equation-solving puzzle game with GitHub Pages deployment, responsive play, and a v0 scope of one graph plus one draggable tile that draws a line.

## 2026-04-05

- Bootstrapped a new Vite + TypeScript project in `/Users/noahsug/Dropbox/programming/code/graphbound`.
- Chose a custom canvas renderer with `roughjs` instead of a sprite engine because the gameplay is graph-first and geometry-heavy.
- Added `README.md`, `PLAN.md`, `PROGRESS.md`, and GitHub Pages deployment scaffolding.
- Confirmed GitHub CLI auth and Node/npm availability before repo creation.
- Installed baseline dependencies and verified the scaffold builds successfully.
- Created and pushed the public repo: `https://github.com/noahsug/graphbound`.
- Installed `roughjs` and `playwright` for the first playable slice and browser verification.
- Replaced the starter Vite page with a canvas-based v0 prototype: one graph board, one `x` tile, one equation slot, one plotted line, one goal gate, and success feedback.
- Added both direct drag interaction and a tap-to-place fallback for mouse/touch ergonomics and automation coverage.
- Added `window.render_game_to_text` and `window.advanceTime(ms)` hooks expected by the game-development workflow.
- Verified the v0 solve flow in a browser using the skill Playwright client. Latest artifacts: `output/web-game/v0-smoke/shot-0.png` and `output/web-game/v0-smoke/state-0.json`.
- Browser smoke test result: solved state reached, no console error artifact emitted, and the screenshot visually matched the intended single-board puzzle.
- Environment gotcha for this Codex session: the skill client required `/Users/noahsug/.codex/node_modules` to resolve `playwright`, so a symlink to the project `node_modules` was created locally outside the repo.
- Enabled GitHub Pages with workflow deployment for `https://noahsug.github.io/graphbound/`.
- First deploy attempt failed on Linux because Vite's transitive wasm runtime peers (`@emnapi/core` and `@emnapi/runtime`) were not explicit in the project. Added them as dev dependencies and confirmed both `npm run build` and `npm ci` now pass.
- Replaced the single-board prototype with a deterministic multi-section architecture for v1.
- Fixed the rough.js redraw wobble by giving the hand-drawn primitives stable seeds, so holding or dragging a tile no longer makes the whole screen visually jitter.
- Implemented the v1 linear progression chain: `Sprout -> Ridge -> Cove`, with unlockable tiles `2`, `+`, and `5`.
- Added multi-slot equations, section-specific rewards, and persistent unlocked-section state.
- Browser-verified the full v1 chain with the Playwright game client. Latest artifacts: `output/web-game/v1-flow/shot-0.png` and `output/web-game/v1-flow/state-0.json`.
- Committed the v1 checkpoint as `feat: implement v1 progression and stable sketch rendering`.
- Extended the game into the first full open-world slice: map camera, floating graph islands, Orchard's two-goal revisit branch, and the `Sprout -> Ridge -> Orchard -> Cove -> Orchard -> Canopy -> Summit` progression.
- Added arrow-key map panning and a small `window.__graphbound_debug` helper so browser automation can focus sections and place tiles deterministically.
- Browser-verified the full branch path with a direct Playwright script. Latest artifacts: `output/web-game/full-debug/shot-0.png` and `output/web-game/full-debug/state-0.json`.
- Re-ran the standard game skill client after the final code changes to keep the required screenshot/state loop in place. Latest baseline artifact: `output/web-game/final-client/shot-0.png`.
- Pending next: push the final full-game pass, and if we continue later, start on obstacles, richer equation grammars, and the level-editor data workflow.
- Reworked the presentation around a single full-screen sky world: removed the old map-plus-board HUD structure and kept only floating tile chips at the bottom of the screen.
- Moved graph boards directly into world space so the start of the game matches the reference more closely: one floating board in view and one tile at the bottom.
- Added connector geometry to goal definitions so exits can behave like actual lines instead of border areas.
- Implemented fuse-style solve animation from the plotted line out to a lock icon, plus a drop-from-the-sky reveal and camera pan for newly unlocked boards.
- Updated the first progression beat so level 2 appears as a physically connected neighboring board after solving level 1.
- Refined the board proportions so the minimal `y =` row has breathing room below the graph on desktop and mobile.
- Browser-verified the simplified start screen and level-2 unlock with the required Playwright game client. Latest artifacts: `output/web-game/minimal-start-v2/shot-0.png` and `output/web-game/minimal-unlock-v2/shot-0.png`.
- Ran an extra mobile-sized browser check and confirmed the simplified world-first layout still reads on a phone viewport. Latest artifacts: `output/web-game/mobile-check/start.png` and `output/web-game/mobile-check/unlock.png`.
- Removed slot-specific tile restrictions so any unlocked tile can be dropped into any open equation box.
- Updated expression parsing so adjacent digits concatenate, adjacent values imply multiplication, and leading or trailing `+` tiles are ignored instead of invalidating the expression.
- Browser-verified the unrestricted-slot behavior on the three-slot board. Example outputs are recorded in `output/web-game/unrestricted-examples/examples.json`, and the affordance screenshot showing all slots highlighted for a selected tile is `output/web-game/unrestricted-highlight/all-empty-slots-valid.png`.
- Reworked the world shell so unlocked sections are grassy land parcels rather than isolated floating boards.
- Added connected terrain growth between unlocked neighbors so level 2 now visually fuses with level 1 into a shared landmass.
- Kept the graph puzzle readable inside each terrain patch while moving the world closer to the reference art direction.
- Added environmental flavor to the expanding world: tree clusters in the orchard/canopy areas, a river with a bridge in the cove branch, and a small dog in the orchard that can be petted for a heart reaction.
- Browser-verified the connected-world presentation with debug-assisted unlock captures. Latest connected-world artifacts: `output/web-game/connected-debug/start.png`, `output/web-game/connected-debug/unlock.png`, `output/web-game/connected-world/orchard.png`, and `output/web-game/dog-check/pet.png`.
- Generalized section content so each area can now define its own land footprint, board layout, graph frame size, and axis ranges instead of inheriting one global `0..10` square graph.
- Added concrete examples of that flexibility into the shipped world: Ridge now extends below `y = 0`, Cove uses `x = -5..5` and negative `y`, and Canopy uses a tall/narrow graph frame on a taller land parcel.
- Refactored graph rendering and plot evaluation so grid lines, axis labels, goal lines, hit detection, and plot mapping all respect per-section axis ranges.
- Simplified the entire backdrop to a pure gradient sky and removed the old decorative background motifs so the world itself carries the visual interest.
- Re-ran `npm run build`, the required Playwright game client, and custom browser captures for desktop plus mobile verification. Latest artifacts: `output/web-game/shot-0.png`, `output/web-game/variable-world/unlock-ridge.png`, `output/web-game/variable-world/cove.png`, `output/web-game/variable-world/canopy.png`, `output/web-game/variable-world-mobile/start.png`, and `output/web-game/variable-world-mobile/cove.png`.
- Docked connected land parcels edge-to-edge in the authored world layout so unlocks extend the same landmass instead of layering a new parcel on top of the old one.
- Removed parcel-level lift, scale, shadow, and border treatment from the terrain so active focus no longer pulls a connected section away from its neighbors and creates visible blue seams.
- Kept section focus on the graph itself instead of the terrain shell, which preserves seamless land joins during camera pans and unlocks.
- Verified the seamless land join on desktop and mobile. Latest artifacts: `output/web-game/seamless-world-v2/unlock-ridge.png`, `output/web-game/seamless-world-v2/cove-branch.png`, `output/web-game/seamless-client-v2/shot-0.png`, and `output/web-game/seamless-mobile/unlock-ridge.png`.
- Moved unlock icons onto the current land parcel edge that faces the next section, while keeping them outside the graph frame itself.
- Changed the fuse route to terminate at the unlock icon instead of continuing beyond it, so the icon is the actual endpoint of the solve animation.
- Hid the unlock icon as soon as the fuse completes and let the newly unlocked parcel start its drop-in reveal immediately after that impact beat.
- Browser-verified the full level-1 sequence with real clicks plus deterministic time stepping. Latest artifacts: `output/web-game/lock-sequence/start.png`, `output/web-game/lock-sequence/mid-fuse.png`, `output/web-game/lock-sequence/unlock-drop.png`, and `output/web-game/shot-0.png` (the standard client still wrote to the root `output/web-game/shot-*.png` files in this environment).
- Retuned the orchard and summit goal targets so the authored `+`-based solves now line up with normal addition layouts like `x + 2` and `x + 5`, instead of accidental dangling-plus variants such as `2 x +`.
- Raised Orchard and Summit to a `y = 0..12` graph range so `x + 5` lands on a different top-edge coordinate than `2x`, which removes the old overlap.
- Narrowed Canopy to `x = -4..4` so the section still clearly asks for the `5` tile rather than letting `x` mimic the same right-edge result.
- Verified the current weird-vs-canonical cases in the browser: `2 x +` no longer unlocks the orchard canopy route or solves summit, while `x + 5` still does. Latest artifacts: `output/web-game/canonical-audit/orchard-weird-fail.png`, `output/web-game/canonical-audit/orchard-canonical-success.png`, `output/web-game/canonical-audit/summit-weird-fail.png`, `output/web-game/canonical-audit/summit-canonical-success.png`, and `output/web-game/canonical-audit/summary.json`.
- Added desktop wheel / two-finger scroll panning on the canvas so trackpads can move the world directly, using the same camera space as drag panning.
- Replaced the old discrete keydown camera jumps with held-key camera velocity, so `WASD` and arrow keys now move continuously frame to frame instead of stepping in chunky increments.
- Tightened direction changes and release damping so switching axes does not leave large sideways drift, and blur now clears movement input cleanly.
- Re-ran `npm run build`, the required Playwright game client, and a targeted desktop browser pass for wheel plus held-key movement. Latest artifacts: `output/web-game/desktop-input-client-v3/shot-0.png`, `output/web-game/desktop-input-client-v3/state-0.json`, `output/web-game/desktop-camera-controls-v3/start.png`, `output/web-game/desktop-camera-controls-v3/after-wheel.png`, `output/web-game/desktop-camera-controls-v3/after-wasd.png`, `output/web-game/desktop-camera-controls-v3/after-arrow.png`, and `output/web-game/desktop-camera-controls-v3/summary.json`.
- Reworked the art direction from pixelated grass cards into a chalkboard scene with hand-drawn paper puzzle boards, rough.js hatch-filled land, rough-filled tiles, and a darker classroom-style backdrop.
- Replaced the old blocky terrain texture with deterministic rough.js hatch patterns so the land looks chalked onto the board instead of digitally tiled.
- Added hand-drawn decorative doodles on the land, alongside sketchier trees, river shapes, bridge planks, and the pettable dog so exploration feels more playful without using any image assets.
- Put the equation on its own bright paper strip below the graph and changed the graph grid / frame treatment to rough hand-drawn lines so the puzzle remains readable inside the new chalkboard world.
- Verified the new renderer is deterministic by panning away and back, then comparing the returned screenshot to the original. The hashes matched exactly, confirming no camera-driven texture jitter.
- Re-ran `npm run build`, the required Playwright game client, a start + unlock browser pass, and the stability check. Latest artifacts: `output/web-game/chalkboard-client-v2/shot-0.png`, `output/web-game/chalkboard-client-v2/state-0.json`, `output/web-game/chalkboard-world-v2/start.png`, `output/web-game/chalkboard-world-v2/unlock-ridge.png`, `output/web-game/chalkboard-world-v2/state.json`, `output/web-game/chalkboard-stability/start.png`, `output/web-game/chalkboard-stability/returned.png`, and `output/web-game/chalkboard-stability/summary.json`.
- Increased the on-screen world scale so graph boards render much larger on desktop, making the play area feel closer to the “zoomed in on one puzzle” framing the user wanted.
- Enlarged tick-label and axis-letter sizing so graph numbers are easy to read against the hand-drawn paper board.
- Re-ran `npm run build`, the required Playwright game client, and a start + unlock browser pass for the larger-board version. Latest artifacts: `output/web-game/bigger-graph-client/shot-0.png`, `output/web-game/bigger-graph-client/state-0.json`, `output/web-game/bigger-graph-world/start.png`, `output/web-game/bigger-graph-world/unlock-ridge.png`, and `output/web-game/bigger-graph-world/state.json`.
- Finished the direct level-start boot flow so `?level=N`, `?level=level-N`, and section-id URLs preload the requested puzzle for QA and sharing.
- The level-start override now treats earlier indexed sections as solved, grants their reward tiles, unlocks their connected paths, and teleports the camera directly to the requested board on load.
- Added debug-state reporting for `activeLevel` and `startLevelOverride`, plus a matching `window.__graphbound_debug.startAtLevel()` helper for browser automation.
- Re-ran `npm run build`, the required Playwright game client, and a dedicated direct-link browser audit for `?level=1`, `?level=4`, `?level=6`, and `?level=orchard`. Latest artifacts: `output/web-game/level-url-client/shot-0.png`, `output/web-game/level-url-client/state-0.json`, `output/web-game/level-direct-links/level-1.png`, `output/web-game/level-direct-links/level-4.png`, `output/web-game/level-direct-links/level-6.png`, and `output/web-game/level-direct-links/summary.json`.
- Reworked the visual language into a much more minimal black-and-white paper style: textured off-white paper background, floating graph axes with pencil ticks, no land parcels, no graph borders, and equations separated cleanly from the axes.
- Retuned goal rendering so every route keeps a visible colored stub before solve, and completed equations use that same color for the plotted line and the extended connection into the next graph’s entry axis.
- Simplified tiles into light-yellow cutout paper pieces with pencil lettering, while keeping drag-and-drop and tap placement intact.
- Stripped out the chalkboard/terrain scenery pass for this iteration so the world now reads as sparse math sketches on a single sheet of paper.
- Re-ran `npm run build`, the required Playwright game client, and focused browser captures for start, early progression, and the branching orchard state. Latest artifacts: `output/web-game/pencil-paper-client/shot-0.png`, `output/web-game/pencil-paper-client/state-0.json`, `output/web-game/pencil-paper-overhaul/start.png`, `output/web-game/pencil-paper-overhaul/sprout-solved.png`, `output/web-game/pencil-paper-overhaul/orchard-start.png`, `output/web-game/pencil-paper-branch/after-cove-unlock.png`, and `output/web-game/pencil-paper-overhaul/summary.json`.
- Raised the minimum world scale on narrow screens so the stripped-down graph presentation stays readable on mobile instead of collapsing into too much whitespace.
- Re-ran `npm run build`, the required Playwright game client, and a desktop plus mobile follow-up pass after the scale tweak. Latest artifacts: `output/web-game/pencil-paper-client-v3/shot-0.png`, `output/web-game/pencil-paper-check-v2/start.png`, `output/web-game/pencil-paper-check-v2/sprout-solved.png`, `output/web-game/pencil-paper-mobile-v3/start.png`, and `output/web-game/pencil-paper-mobile-v3/summary.json`.
- Removed the remaining white equation strip so equations now sit directly on the paper background, and adjusted the equation layout logic so the text row stays above or below the graph without overlapping it.
- Restyled open slots into light-yellow dashed placeholder boxes, and tightened the rough fill on the `+` tile so it reads as a denser cut-paper piece than the looser hatch fills.
- Reworked connector routing so solved goal lines aim for real axis endpoints on the next graph and detour around other graph bodies before being smoothed, which keeps the routes from cutting through neighboring puzzles.
- Replaced the flat paper wash with a deterministic repeating paper texture tile plus subtle smudges, keeping the background more interesting while still staying stable during camera movement.
- Re-ran `npm run build`, the required Playwright game client, and focused desktop/mobile browser checks for the new layout and connector routing. Latest artifacts: `output/web-game/pencil-paper-client-v4/shot-0.png`, `output/web-game/pencil-paper-client-v4/state-0.json`, `output/web-game/pencil-paper-routes-v1/start.png`, `output/web-game/pencil-paper-routes-v1/orchard-start.png`, `output/web-game/pencil-paper-polish/orchard-after-canopy.png`, and `output/web-game/pencil-paper-polish/mobile-start.png`.
- Swapped the old unlock drop behavior for an in-place sketch reveal: newly unlocked graphs now draw in over time through staged axis, tick, goal-line, and equation appearance instead of falling from the sky.
- Changed connector rendering so route color fades toward dark graphite near the graph it unlocks, both for the short visible stubs and the fully completed link path.
- Added an internal `window.__graphbound_debug.animatePlaceTile()` helper so browser QA can trigger the real animated solve path deterministically.
- Re-ran `npm run build`, the required Playwright game client, and targeted browser captures for the first unlock animation. Latest artifacts: `output/web-game/graph-reveal-client/shot-0.png`, `output/web-game/graph-reveal-client/state-0.json`, `output/web-game/graph-reveal-live-v2/unlock-mid.png`, `output/web-game/graph-reveal-live-v2/unlock-settled.png`, and `output/web-game/graph-reveal-live-v3/unlock-drawing.png`.
- Reworked graph selection so whichever graph sits closest to screen center becomes the active puzzle automatically, instead of selection being tied to the last graph clicked.
- Tied the tray to that selected graph’s remaining unused tiles, so panning the camera can change the tray contents even without clicking.
- Changed empty-slot highlighting to stay active on the selected graph, which makes it obvious where tiles can be dropped as soon as a graph comes into focus.
- Re-ran `npm run build`, the required Playwright game client, and a focused selection-control browser pass. Latest artifacts: `output/web-game/selection-client/shot-0.png`, `output/web-game/selection-client/state-0.json`, `output/web-game/selection-check-v2/ridge-selected.png`, `output/web-game/selection-check-v2/sprout-selected-by-scroll.png`, and `output/web-game/selection-check-v2/ridge-selected-by-click.png`.

## 2026-04-08

- Fixed the connector renderer so it no longer draws the raw cornered waypoint polyline on top of the smoothed route. The rough pass now samples along the actual curve, which removes the extra elbow artifacts the user called out.
- Verified the fix with `npm run build`, the required Playwright game client, and a debug-assisted solved-state browser capture. Latest artifacts: `output/web-game/shot-0.png`, `output/web-game/state-0.json`, `output/web-game/line-smooth-fix.png`, and `output/web-game/line-smooth-fix-state.json`.
- Fixed goal-shape placement so targets now sit on the actual graph hit anchor instead of the first external route waypoint. That keeps puzzle goals on the line the player is trying to reach, including Ridge/puzzle 2.
- Re-ran `npm run build`, the required Playwright game client, and focused browser captures for Ridge. Latest artifacts: `output/web-game/shot-0.png`, `output/web-game/state-0.json`, `output/web-game/goal-shape-anchor-ridge-unsolved.png`, and `output/web-game/goal-shape-anchor-ridge-unsolved-state.json`.
- Tightened equation spacing for fixed-token layouts by shrinking the `y =` prefix gap and giving fixed symbols like `x` and `+` compact text widths instead of full slot widths. Puzzle 2 now reads much closer to the tighter puzzle-3 spacing.
- Re-ran `npm run build`, the required Playwright game client, and a Ridge-specific browser capture for the equation layout. Latest artifacts: `output/web-game/shot-0.png`, `output/web-game/state-0.json`, `output/web-game/equation-spacing-ridge.png`, and `output/web-game/equation-spacing-ridge-state.json`.
- Moved Ridge slightly closer to Sprout in the authored world layout so the first two puzzles feel more connected without changing the overall scale or camera framing.
- Re-ran `npm run build`, the required Playwright game client, and a wide browser capture centered on Sprout to verify the tighter spacing. Latest artifacts: `output/web-game/shot-0.png`, `output/web-game/state-0.json`, `output/web-game/first-two-closer.png`, and `output/web-game/first-two-closer-state.json`.
- Removed the remaining single-use tile rules: the tray no longer hides tiles after placement, and placing a tile into one slot no longer clears that same symbol out of another slot.
- Verified duplicate placement with Orchard by putting `2` into two equation slots at once while the tray still showed its own `2`. Latest artifacts: `output/web-game/shot-0.png`, `output/web-game/state-0.json`, `output/web-game/reusable-tiles-orchard.png`, and `output/web-game/reusable-tiles-orchard-state.json`.
- Added camera clamping based on the currently unlocked graphs so drag, wheel, keyboard motion, resize, and camera centering all keep at least one graph meaningfully visible on screen. As more graphs unlock, the allowed camera region expands with them.
- Verified the clamp with extreme wheel and keyboard movement from both the starting state and a later unlocked state. Latest artifacts: `output/web-game/shot-0.png`, `output/web-game/state-0.json`, `output/web-game/camera-clamp-start.png`, `output/web-game/camera-clamp-start-state.json`, `output/web-game/camera-clamp-level3.png`, `output/web-game/camera-clamp-level3-state.json`, `output/web-game/camera-clamp-keyboard.png`, and `output/web-game/camera-clamp-keyboard-state.json`.
- Switched camera focus from section origins to actual graph centers, so the first graph now loads centered and click-to-center lands the graph itself in the intended framing.
- Verified the new framing on mobile-sized captures for initial load and real click recentering. Latest artifacts: `output/web-game/shot-0.png`, `output/web-game/state-0.json`, `output/web-game/graph-centered-start.png`, `output/web-game/graph-centered-offset-click2.png`, `output/web-game/graph-centered-click2.png`, and `output/web-game/graph-centered-click2-state.json`.
- Moved the active equation row into screen space so it stays in one fixed on-screen position while the camera pans, instead of drifting with the graph world.
- Verified the fixed equation position with before/after pan captures plus the standard game client loop. Latest artifacts: `output/web-game/shot-0.png`, `output/web-game/state-0.json`, `output/web-game/equation-fixed-start.png`, `output/web-game/equation-fixed-offset.png`, and `output/web-game/equation-fixed-state.json`.
- Restyled the `+` tile to use the same paper-cut cross-hatch background treatment as the other tiles, while keeping it a slightly different warm color so it still reads as distinct.
- Re-ran `npm run build`, the required Playwright game client, and an Orchard tray capture to verify the new `+` tile look. Latest artifacts: `output/web-game/shot-0.png`, `output/web-game/state-0.json`, `output/web-game/plus-tile-paper-match.png`, and `output/web-game/plus-tile-paper-match-state.json`.
- Replaced the remaining legacy connector waypoint behavior with anchor-first routing: solved links now leave the goal shape in the correct outward direction, route around graph rectangles instead of old board boxes, and treat other visible goal shapes as obstacles.
- Added self-intersection scoring when choosing the next-graph axis approach so the connector prefers the cleanest shortest route and avoids the looping backtrack the user highlighted near Ridge.
- Verified the loop fix with `npm run build`, the required Playwright game client, and targeted Ridge-to-Orchard browser captures across the solve timeline. Latest artifacts: `output/web-game/shot-0.png`, `output/web-game/state-0.json`, `output/web-game/connector-loop-fix/ridge-solved.png`, `output/web-game/connector-loop-fix/state.json`, `output/web-game/connector-loop-timeline/frame-300.png`, `output/web-game/connector-loop-timeline/frame-700.png`, and `output/web-game/connector-loop-timeline/frame-1100.png`.
- Relaxed the camera clamp so any visible graph or revealed connector line counts as valid world content. The clamp now uses overlap-based visibility rects for graph bodies plus solved/animating connector bounds, instead of requiring a graph center to stay on screen.
- Verified the new pan space with a level-2 browser pass that scrolls into the Sprout↔Ridge gap and then farther left until the clamp stops. Latest artifacts: `output/web-game/camera-connector-gap/start-level2.png`, `output/web-game/camera-connector-gap/mid-gap.png`, `output/web-game/camera-connector-gap/clamped-left.png`, `output/web-game/camera-connector-gap/state.json`, plus the standard client loop artifacts `output/web-game/shot-0.png` and `output/web-game/state-0.json`.
- Moved the active equation row back into graph-relative world space so it stays attached to its graph while the camera pans, instead of hovering in a fixed viewport position.
- Kept the newer compact token spacing while restoring board-relative centering and below-graph placement logic for the equation row.
- Verified the equation anchoring with a before/after pan browser pass and the standard Playwright client loop. Latest artifacts: `output/web-game/equation-attached/start.png`, `output/web-game/equation-attached/after-pan.png`, `output/web-game/equation-attached/state.json`, plus the standard client loop artifacts `output/web-game/shot-0.png` and `output/web-game/state-0.json`.
- Changed connector reveal timing so it advances along the actual rendered curve instead of the raw waypoint path, which removes the little hesitation near the goal shape and keeps the tip motion visually even.
- Switched fuse duration from a fixed constant to a curve-length-based duration, so long connectors no longer shoot across the page faster than short ones.
- Verified the new connector speed with a first-level solve timeline and the standard Playwright client loop. Latest artifacts: `output/web-game/connector-speed/frame-250.png`, `output/web-game/connector-speed/frame-600.png`, `output/web-game/connector-speed/frame-1000.png`, `output/web-game/connector-speed/frame-1500.png`, `output/web-game/connector-speed/state.json`, plus the standard client loop artifacts `output/web-game/shot-0.png` and `output/web-game/state-0.json`.
- Reworked the paper backdrop into notebook-style writing paper by adding world-stable horizontal rules and faint repeating margin guides over the existing textured sheet.
- Added deterministic pencil doodles and stray marks in world space, with candidate placement rejected against all board areas and revealed connector routes so the background art never sits on top of graphs, equations, or graph lines.
- Kept all new background art monochrome and low-contrast so it reads as paper ambience instead of competing with gameplay.
- Verified the new background look with focused level-2 browser captures and the standard Playwright client loop. Latest artifacts: `output/web-game/writing-paper-bg/start-level2.png`, `output/web-game/writing-paper-bg/gap-pan.png`, `output/web-game/writing-paper-bg/solved-gap.png`, `output/web-game/writing-paper-bg/state.json`, plus the standard client loop artifacts `output/web-game/shot-0.png` and `output/web-game/state-0.json`.
- Removed the connector’s separate pixels-per-ms constant and now derive its duration from the solved graph’s actual on-screen plot speed, so the post-shape travel uses the same pace as the graph line that led into the target.
- Kept the connector reveal on the rendered curve path, but changed the duration math to `connector length / plot speed` instead of using an independent connector speed.
- Verified the speed match with fresh first-goal handoff captures and the standard Playwright client loop. Latest artifacts: `output/web-game/connector-speed-handoff/frame-650.png`, `output/web-game/connector-speed-handoff/frame-850.png`, `output/web-game/connector-speed-handoff/frame-1050.png`, `output/web-game/connector-speed-handoff/frame-1250.png`, `output/web-game/connector-speed-match/frame-700.png`, `output/web-game/connector-speed-match/frame-1250.png`, and the standard client loop artifacts `output/web-game/shot-0.png` and `output/web-game/state-0.json`.
- Made equation placement collision-aware by testing candidate horizontal anchors against the graph’s currently visible connector routes and choosing the first row position that stays clear.
- This lets the equation row slide right or left when needed, instead of allowing a solved connector to cross over the text and slots.
- Verified the fix on Orchard’s lower route with a focused browser capture and the standard Playwright client loop. Latest artifacts: `output/web-game/equation-avoid-route/orchard-cove-route.png`, `output/web-game/equation-avoid-route/state.json`, plus the standard client loop artifacts `output/web-game/shot-0.png` and `output/web-game/state-0.json`.
- Tied the camera follow to the live connector tip during fuse travel, so the screen now moves with the post-shape line instead of waiting for a separate pan after the connector is already racing across the page.
- Added per-section runtime state for the connector-tip screen anchor and now derive the camera position directly from the same `fuseProgress` used to reveal the connector.
- Verified the sync with `npm run build`, the required Playwright game client, and fresh fixed-time browser captures. Latest artifacts: `output/web-game/camera-line-sync/frame-650.png`, `output/web-game/camera-line-sync/frame-900.png`, `output/web-game/camera-line-sync/state.json`, `output/web-game/camera-line-sync-abs/frame-500.png`, `output/web-game/camera-line-sync-abs/frame-800.png`, and `output/web-game/camera-line-sync-abs/frame-1100.png`.
- Added user-controlled zoom with persistent zoom level across resize: `Q` zooms out, `E` zooms in, `Cmd`/trackpad gesture zooms around the cursor, and mobile pinch zooms around the pinch midpoint while also allowing the midpoint to pan.
- Refactored layout scale into a viewport base scale plus user zoom multiplier, so zoom changes stay in the same world/camera coordinate system instead of stretching the canvas or breaking hit testing.
- Verified zoom with `npm run build`, the required Playwright game client, desktop keyboard and `Cmd`-wheel captures, plus a Chromium CDP touch-pinch pass. Latest artifacts: `output/web-game/zoom-controls/start.png`, `output/web-game/zoom-controls/after-e.png`, `output/web-game/zoom-controls/after-meta-wheel.png`, `output/web-game/zoom-controls/after-meta-wheel-state.json`, `output/web-game/zoom-pinch-cdp/after-pinch-out.png`, and `output/web-game/zoom-pinch-cdp/state.json`.
- Expanded the notebook-paper background art with more deterministic pencil doodles and a new larger “artwork” layer, adding motifs like flowers, clouds, kites, leaves, and little paper-boat sketches on top of the existing ambient marks.
- Increased the density of small and medium background doodles so the page feels more playful even in the opening camera framing, while keeping the same collision filtering so nothing is drawn over graphs, equations, or visible connector lines.
- Changed the solve handoff so the black plotted graph now stops at the target shape instead of continuing to the next graph as part of the same stroke.
- Completed targets now activate visually with a filled-in colored shape, and the unlock path is drawn as a separate colored connector that starts just outside the target and travels toward the next graph.
- Re-ran `npm run build`, the required Playwright game client, and a focused browser capture of the first-board solve handoff. Latest artifacts: `output/web-game/colored-goal-handoff/start.png`, `output/web-game/colored-goal-handoff/plot-hit.png`, `output/web-game/colored-goal-handoff/connector-mid.png`, `output/web-game/colored-goal-handoff/connector-end.png`, `output/web-game/colored-goal-handoff/start.json`, and `output/web-game/colored-goal-handoff/console-errors.json`.
- Verified the richer background with `npm run build`, the required Playwright game client, and fresh browser captures at the start view plus level-2 pans. Latest artifacts: `output/web-game/background-art-pass-v2/start.png`, `output/web-game/background-art-pass-v2/level2.png`, and `output/web-game/background-art-pass-v2/level2-pan-right.png`.
- Tightened the authored world spacing by moving Ridge, Orchard, Cove, Canopy, and Summit closer together, with the biggest compression applied to the first three graphs so progression feels denser earlier.
- Kept the same branch structure and puzzle framing while shrinking the horizontal and vertical gaps enough to reduce dead space between connected boards.
- Verified the new spacing with `npm run build`, the required Playwright game client, and direct browser captures for the start view plus later deep-linked levels. Latest artifacts: `output/web-game/closer-graphs/start.png`, `output/web-game/closer-graphs/level2.png`, `output/web-game/closer-graphs/level3.png`, and `output/web-game/closer-graphs/level6.png`.
- Simplified empty equation slots to a plain dashed black outline with no fill and no warm highlight tint, so the placeholders read as neutral boxes on the page instead of colored paper chips.
- Left placed tiles unchanged; only the empty equation boxes were restyled.
- Verified the new slot look with `npm run build`, the required Playwright game client, and a level-3 browser capture. Latest artifacts: `output/web-game/slot-outline-style/level3-empty-slots.png` and `output/web-game/slot-outline-style/state.json`.
- Moved the active equation row back under the selected graph in world space, so it sits on the paper background below the graph instead of floating as a HUD row above the tray.
- The active equation now stays fixed relative to the background and graph, and it no longer flips above the graph; it always renders below.
- Verified the under-graph placement with `npm run build`, the required Playwright game client, and a before/after pan capture on level 3. Latest artifacts: `output/web-game/equation-under-graph/start.png`, `output/web-game/equation-under-graph/after-pan.png`, and `output/web-game/equation-under-graph/after-pan-state.json`.
- Capped the maximum user zoom at the startup framing, so keyboard zoom-in, pinch zoom-in, and `Cmd`/trackpad zoom can only return to the initial view and never go closer.
- Moved goal-shape rendering into a global world pass so every target shape exists from the start of the game, even for still-locked graphs.
- Locked graph targets now render in a neutral black-and-white graphite style, and each target switches to its authored color as soon as that graph becomes unlocked while keeping the completion fill behavior.
- Removed target-like symbols from the random background doodle pools and added goal-shape bounds to the doodle obstacle pass, so background marks no longer generate fake target hearts/stars/circles/x’s or overlap the real ones.
- Verified the change with `npm run build`, the required Playwright game client, and focused browser captures for the opening state plus the first unlock. Latest artifacts: `output/web-game/goal-shapes-global/start.png`, `output/web-game/goal-shapes-global/after-first-unlock.png`, `output/web-game/goal-shapes-global/start.json`, `output/web-game/goal-shapes-global/after-first-unlock.json`, and `output/web-game/goal-shapes-global/console-errors.json`.
- Replaced the old post-target unlock connector with a separate dashed background route that starts away from the filled target instead of touching it, matching the new “path on the paper” direction.
- The unlock path now uses a lighter version of the target color, a wider hand-drawn dashed stroke, and the same reveal timing/camera follow logic as the graph-line handoff so the route draw and pan move together.
- Verified the dashed route with `npm run build`, the required Playwright game client, and a focused first-unlock timeline. Latest artifacts: `output/web-game/dashed-unlock-path/plot-hit.png`, `output/web-game/dashed-unlock-path/dash-mid.png`, `output/web-game/dashed-unlock-path/dash-end.png`, `output/web-game/dashed-unlock-path/start.json`, and `output/web-game/dashed-unlock-path/console-errors.json`.
- Split the success animation into three explicit phases: graph line draw first, then target-shape fill, then dashed route reveal with camera follow.
- Added a short dedicated target-fill timing phase between plot completion and route reveal so the target can visually “pop” before the camera starts moving.
- Verified the phased sequence with `npm run build`, the required Playwright game client, and a timed browser timeline. Latest artifacts: `output/web-game/success-sequence/plot-mid.png`, `output/web-game/success-sequence/plot-finished.png`, `output/web-game/success-sequence/target-filling.png`, `output/web-game/success-sequence/target-filled.png`, `output/web-game/success-sequence/dash-mid.png`, and `output/web-game/success-sequence/console-errors.json`.
- Changed drag-and-drop slot targeting so placement is based on the dragged tile’s visual center instead of the mouse cursor hotspot, which makes off-corner grabs behave naturally when dropping into equation slots.
- Moved dashed unlock paths so they start just outside the current graph edge and end just outside the next graph edge, instead of appearing to grow out of the target shape or into the next graph.
- Lightened the locked black-and-white target shapes so unreached goals stay visible from the opening view without competing as strongly with unlocked colored targets.
- Reworked dashed unlock routing to follow the line between graph centers, with the visible path clipped to begin and end just outside each graph body instead of hugging the top edge or attaching to interior graph anchors.
- Removed the old post-route camera tween and changed dashed unlock travel to use the same progress value for both line reveal and camera pan, so the camera now travels with the dashed path instead of waiting for a second “scroll to next graph” pass afterward.
- Split the target-fill and dashed-route phases more cleanly so the filled target gets its own animation beat before the dashed route starts advancing.
- Normalized tile paper colors by role so variables share one background color, numbers share one, and operators share one instead of each individual symbol using its own separate tint.
- Added a dedicated `PUZZLE_BRAINSTORMING_PLAN.md` with equation-family ideas, unlock-order suggestions, revisit strategy, and a recommended difficulty arc from simple line puzzles through absolute value, quadratics, logs, trig, piecewise functions, and polar space.
- Added `GRAPH_AUTHORING_PLAN.md` describing how to make graph add/remove/move work cheaper by separating puzzle content from world layout, introducing graph visual presets, and eventually adding a drag-to-reposition debug authoring mode.
- Expanded the live content set from 6 graphs to 20 authored graphs while keeping the first three puzzles intact, and rewired the later world into a broader branch structure with cove/basin/gallery, canopy, a central crossroads board, west-branch revisits, and a longer north-endgame chain.
- Added new unlockable tiles `-`, `1`, and `0`, and extended equation parsing so subtraction works alongside the existing digit-concatenation and implicit-multiplication rules.
- Kept tile rewards gated behind full graph completion and authored the new progression around that rule, so branch hub graphs like Orchard and Crossroads can stay partially complete for long stretches before finally paying out or fully clearing.
- Tightened a number of later puzzle templates by replacing overly-open slot patterns with more fixed equation fragments after brute-force authoring checks exposed accidental target hits.
- Updated debug bootstrapping so deep-link starts and later-level previews respect infinite tile reuse instead of assuming each unlocked symbol can only be consumed once.
- Verified the content pass with `npm run build`, the required Playwright game client, a deterministic branch test that reaches Hollow, and a longer endgame automation that reaches Finale with the new tile set. Latest artifacts: `output/web-game/puzzle20-client/shot-0.png`, `output/web-game/puzzle20-playthrough-v2/finale.png`, `output/web-game/puzzle20-playthrough-v2/state.json`, and `output/web-game/puzzle20-playthrough/state.json`.
- Follow-up note: the central four-target Crossroads graph is working in live play, but it is still the loosest-authored board in the set because the game intentionally allows any tile in any slot. If we want even tighter “only the intended formula works” guarantees there, the next pass should give that board a more constrained fixed-token equation template rather than relying only on edge-position tuning.
- Slowed the unlock camera pan without changing the dashed connector timing by splitting the camera onto its own longer progress track while keeping the connector reveal on its existing linear timing.
- The unlock camera still uses eased interpolation, but it now takes longer to arrive, which makes puzzle-complete pans feel calmer and less abrupt while preserving the connector’s current constant-speed draw.
- Verified the camera-only change with `npm run build`, the required Playwright game client, and a focused first-unlock timeline. Latest artifacts: `output/web-game/slow-camera-client/shot-0.png`, `output/web-game/slow-camera-check/frame-900.png`, `output/web-game/slow-camera-check/frame-1600.png`, `output/web-game/slow-camera-check/frame-2500.png`, and `output/web-game/slow-camera-check/state.json`.
- Spread the authored world layout back out so every connected graph pair now sits in roughly the same longer spacing band, which keeps dashed unlock routes from collapsing into tiny hops.
- The connected-pair center distances are now in the `520px` to `759px` range in authored world space, which yields visibly longer unlock lines without exploding the overall map scale.
- Changed connector rendering to honor routed waypoints exactly instead of smoothing multi-segment detours, so a path that routes around a graph can no longer curve back through it during rendering.
- Verified the spacing and route-render pass with `npm run build`, the required Playwright game client, and direct browser captures. Latest artifacts: `output/web-game/spacing-client/shot-0.png`, `output/web-game/spacing-check-v2/sprout-ridge-link.png`, `output/web-game/spacing-check-v2/orchard-cove-link.png`, `output/web-game/spacing-check-v2/crossroads-layout.png`, and `output/web-game/spacing-check-v2/state.json`.
- Switched graph sizing to a true “world units per graph unit” model so axis span now directly controls the physical graph size instead of every board reusing a fixed graph frame.
- Horizontal and vertical graph size now come from the section axis ranges using the default board as the baseline scale, which means a `0..20` graph will render twice as wide as a `0..10` graph and a `0..2` graph will render much narrower.
- Kept authored graph placement stable by preserving each section’s existing graph-center offset from its world anchor, then rebuilding board and terrain rects outward from the new dynamic graph size.
- Updated equation placement and token layout scaling to follow the dynamic graph rects instead of the old fixed board dimensions, and updated debug state output to report the real rendered graph size.
- Verified the unit-sizing change with `npm run build`, the required Playwright game client, and deep-link browser captures. Latest artifacts: `output/web-game/unit-size-level1/shot-0.png`, `output/web-game/unit-size-level5/shot-0.png`, `output/web-game/unit-size-level7/shot-0.png`, `output/web-game/unit-size-level8/shot-0.png`, and the matching `state-0.json` files showing `42px`, `105px`, `210px`, and `252px` graph widths for different x-axis spans.
- Added canonical-solution metadata to authored goal shapes and a content-load assertion that throws if any two intended goal solves reuse the same canonical answer.
- Retuned the duplicated later puzzles so the authored solve set is now unique across the world, including Orchard, Gallery, Canopy, Crossroads, and Loft.
- The current intended canonical solve list is: `x`, `x + 2`, `2x`, `x + 5`, `2`, `5x`, `7`, `2x + 5`, `4`, `2 - x`, `1 + x`, `5`, `x^2 + 2`, `x^2 + 5`, `5 - x`, `5 - x^2`, `x^2 - 5`, `-1`, `10`, `10x`, `10 - x`, `10 - x^2`, `x^2 - 10`, and `2x^2 - 10`.
- Verified the uniqueness pass with `npm run build`, a brute-force authoring script that enumerates every slot combination and confirms the reachable solve on each graph, and fresh Playwright deep-link captures for retuned boards. Latest artifacts: `output/web-game/unique-solutions-level3/shot-0.png`, `output/web-game/unique-solutions-level8/shot-0.png`, `output/web-game/unique-solutions-level16/shot-0.png`, and the matching `state-0.json` files.
- Increased the minimum zoom level floor from `0.72` down to `0.18`, which expands the maximum zoom-out range by about 4x while keeping the startup zoom and max zoom-in unchanged.
- Verified the wider zoom-out range with `npm run build` and a direct Playwright browser check that repeatedly pressed `Q` at level 8 until the clamp was reached. Latest artifacts: `output/web-game/zoomout-4x-direct/shot-0.png` and `output/web-game/zoomout-4x-direct/state-0.json`, with the captured state reporting `zoom: 0.18`.
- Added goal inspection guides so hovering or clicking a target shape highlights colored axis ticks on the owning graph and labels the exact target coordinates with at most one decimal place.
- Hover acts as a temporary preview, click pins the same guide, and the debug state now exposes the currently inspected goal key to make browser verification easier.
- Verified the goal-guide interaction with `npm run build` and direct Playwright browser checks that hit a Crossroads target shape. Latest artifacts: `output/web-game/goal-guides-hit/hover.png`, `output/web-game/goal-guides-hit/clicked.png`, `output/web-game/goal-guides-hit/hover-state.json`, and `output/web-game/goal-guides-hit/click-state.json`.
- Added a zoom-sensitive glow cue around unlocked but still-unsolved colored target shapes so they stand out when the camera is pulled far back.
- The glow only appears below the new zoomed-out threshold, uses the target’s own color, and stays subtle enough to act as wayfinding instead of competing with the graph drawings.
- Verified the zoomed-out goal glow with `npm run build` and a direct Playwright zoom-out check at level 8. Latest artifacts: `output/web-game/goal-glow-zoomout/shot-0.png` and `output/web-game/goal-glow-zoomout/state-0.json`, with the captured state reporting `zoom: 0.18`.
- Re-authored the quadratic boards to use a real fixed `x²` token instead of visually showing `x x`, and taught the expression builder to evaluate `x²` as `x * x` so the authored content, solver, and on-screen equation all stay in sync.
- Verified the `x²` pass with `npm run build`, the required Playwright game client against the live local server, and a deep-linked browser capture of Eastreach. Latest artifacts: `output/web-game/x-squared-client/shot-0.png`, `output/web-game/x-squared-client/state-0.json`, and `output/web-game/x-squared-direct/shot-0.png`.
- Extended the equation system from flat token strings to richer notation parts, adding superscripts, subscripts, `sin`, `log`, parentheses, division, absolute-value bars, and a polar `r =` mode with a theta variable.
- Added the `θ` tile, filtered tray contents per section so boards only show compatible tiles, and tightened placement/bootstrap logic to respect each slot’s allowed tile list.
- Re-authored the later world to cover all requested mechanic families with live puzzles: fractions and parentheses on Cove/Basin, absolute value on Gallery/Canopy/South Vault, superscript powers on Eastreach/East Vault, sine on Arch/Mire, logs on Hollow/Southreach/Finale, and polar theta boards on Loft/Weave.
- Fixed two parser/runtime issues during browser QA: absolute-value bars no longer confuse implicit multiplication, and invalid bootstrap combinations now fail safely instead of crashing the app while the deep-link solver probes possibilities.
- Updated `PUZZLE_BRAINSTORMING_PLAN.md` with the newly used puzzle families plus a larger bank of future parentheses, division, abs, trig, exponent, log, and polar ideas for later authoring passes.
- Verified the advanced puzzle pass with `npm run build` and fresh browser captures across representative mechanics. Latest artifacts: `output/web-game/advanced-puzzles-pass-v3/gallery-start.png`, `output/web-game/advanced-puzzles-pass-v3/arch-start.png`, `output/web-game/advanced-puzzles-pass-v3/southreach-solved.png`, `output/web-game/advanced-puzzles-pass-v3/loft-start.png`, `output/web-game/advanced-puzzles-pass-v3/weave-solved.png`, `output/web-game/advanced-puzzles-pass-v3/finale-start.png`, and the matching JSON state files in the same directory.
- Re-ran the required Playwright game client against a fresh local dev server on the polar `Loft` board to confirm the standard automation loop still sees the new notation, theta-only tray filtering, and `r =` equation prefix. Latest artifacts: `output/web-game/advanced-mechanics-client/shot-0.png` and `output/web-game/advanced-mechanics-client/state-0.json`.
- Re-authored the world layout to be denser and more circular, bending the late-game branches back toward the opening area instead of letting the whole progression stretch into one long line.
- Kept the central `Crossroads` board as the hub, let the trig/log routes fold back toward the left side of the map, and left the theta route as the one intentionally longer arm so the overall world reuses space more aggressively.
- Verified the denser layout with `npm run build`, direct zoomed-out browser captures, and the required Playwright game client. Latest artifacts: `output/web-game/dense-layout/crossroads-zoomout.png`, `output/web-game/dense-layout/sprout-zoomout.png`, `output/web-game/dense-layout/crossroads-state.json`, and `output/web-game/dense-layout-client/shot-0.png`.
- Changed goal anchoring to use the terminal matching boundary hit instead of the first one, so target shapes now sit where the plotted solution ends rather than where it first touches the goal band.
- Made plot-point ordering explicit by sorting sampled points from lower `x` to higher `x`, which keeps the line reveal direction consistent with the new “target at the end of the line” rule.
- Verified the endpoint-direction fix with `npm run build`, the required Playwright game client on level 1, and focused browser captures at `output/web-game/goal-end-fix/level1-start.png`, `output/web-game/goal-end-fix/level1-solved.png`, `output/web-game/goal-end-fix/level4-start.png`, plus `output/web-game/goal-end-client/state-0.json`.
- Moved target shapes fully outside their graphs, tightened their footprint, and expanded equation-row collision avoidance so equation placement now steers clear of nearby graphs and target markers instead of only checking connector lines.
- Re-authored a few crowded authored routes at Orchard, Crossroads, Basin, and Arch so the denser circular layout still keeps graphs, equations, and targets from touching each other.
- Added a debug overlap inspector for graph, equation, and target bounds, then used it to verify the authored world now reports zero layout collisions. Latest artifacts: `output/web-game/layout-overlap-fix/orchard.png`, `output/web-game/layout-overlap-fix/crossroads.png`, `output/web-game/layout-overlap-fix/issues.json`, and `output/web-game/layout-overlap-client/shot-0.png`.
- Unified equation text sizing so superscripts and subscripts now use the same readable font size as the rest of the equation instead of shrinking down on exponent and log-base boards.
- Kept the superscript/subscript placement cues by shifting those tokens vertically, while widening their layout boxes so the larger text still fits cleanly.
- Verified the equation typography pass with `npm run build`, focused browser captures at `output/web-game/equation-fontsize-fix/eastvault.png`, `output/web-game/equation-fontsize-fix/southreach.png`, `output/web-game/equation-fontsize-fix/finale.png`, and the required Playwright client at `output/web-game/equation-fontsize-client/shot-0.png`.
- Added a third slot-placeholder state so selecting or dragging a tile now grays out every visible equation box that cannot accept that tile, instead of only highlighting the compatible boxes.
- Made compatibility checks section-aware for rendering, so the same selected tile can correctly mute disallowed slots across other visible graphs as well, including `x` versus `θ` restrictions.
- Verified the slot gray-out behavior with `npm run build`, focused browser captures at `output/web-game/slot-grayouts/x-selected.png` and `output/web-game/slot-grayouts/theta-selected.png`, plus the required Playwright client at `output/web-game/slot-grayouts-client/shot-0.png`.
- Added support for custom displayed equations separate from the underlying solved expression, which lets boards render full left-side / zero-form notation like `y - sin(x + 2) - 2 = 0` without changing how the graph is evaluated.
- Shipped that notation on live puzzles in Mire and Northreach, so the world now includes equations with all terms on the left side and `= 0` on the right.
- Updated `PUZZLE_BRAINSTORMING_PLAN.md` with the shipped left-side examples plus more future zero-form ideas, and verified the notation pass with `npm run build`, focused browser captures at `output/web-game/left-side-equations/mire.png` and `output/web-game/left-side-equations/northreach.png`, plus the required Playwright client at `output/web-game/left-side-client/shot-0.png`.
- Changed puzzle focusing so clicking or otherwise focusing a graph now also restores the camera to the startup zoom level before centering that board, which makes “jump back into play” feel more intentional after zooming far out.
- Kept free zoom behavior unchanged for normal pan/scroll/keyboard navigation; only the explicit puzzle-focus path resets the zoom.
- Verified the focus-zoom reset with `npm run build`, focused browser captures and state dumps at `output/web-game/focus-zoom-reset/before.png`, `output/web-game/focus-zoom-reset/after.png`, `output/web-game/focus-zoom-reset/before.json`, `output/web-game/focus-zoom-reset/after.json`, and the required Playwright client at `output/web-game/focus-zoom-client/shot-0.png`.
- Expanded the target-shape library beyond hearts, circles, stars, and x-marks by adding hand-drawn triangle, square, diamond, hexagon, and clover shapes to the goal renderer.
- Reassigned several live goals to the new shapes so the world actually uses the broader shape vocabulary: Ridge now opens with a triangle, Cove with a square, Gallery with a diamond, Crossroads with a hexagon and triangle branch mix, and Arch/Hollow now use clover and hexagon targets.
- Verified the shape pack with `npm run build`, representative browser captures at `output/web-game/goal-shape-pack/level2/shot-0.png`, `output/web-game/goal-shape-pack/level4/shot-0.png`, `output/web-game/goal-shape-pack/level6/shot-0.png`, `output/web-game/goal-shape-pack/level14/shot-0.png`, `output/web-game/goal-shape-pack/level19/shot-0.png`, and the required Playwright client at `output/web-game/goal-shape-pack-client/shot-0.png`.
